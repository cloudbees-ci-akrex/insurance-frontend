apiVersion: environment.cloudbees.com/v1alpha1
kind: GitRepository
metadata:
  name: cloudbees-ci-akrex-insurance-frontend                             
  namespace: previews
spec:
  url: https://github.com/cloudbees-ci-akrex/insurance-frontend.git
  apiTokenSecretRef:
    name: beedemo-dev-github-token
  autoUpdateContexts:                           
  - default
